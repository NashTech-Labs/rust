/*
use mock_derive::mock;
use scylladb_poc::env_set_up::*;

#[test]
fn keyspace_created() {
    let session= scylladb_poc::env_set_up::connection::connect();
    let keyspace= keyspace::create_keyspace(&session);
}*/
